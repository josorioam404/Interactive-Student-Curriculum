spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/interactive_curriculum
    username: admin
    password: admin123
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

jwt:
  secret: test-secret-key-for-testing-purposes-only-must-be-very-long-for-security
  expiration: 86400000