# Logs (Registros de errores)
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencias (La carpeta más pesada, nunca se sube)
node_modules
.pnp
.pnp.js

# Testing (Reportes de cobertura de pruebas)
coverage

# Build de producción (Lo que genera Vite al compilar)
dist
dist-ssr

# Archivos de entorno (Importante por seguridad)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Archivos temporales de editores y sistema operativo
.DS_Store
Thumbs.db
.idea
.vscode/*
!.vscode/extensions.json
*.suo
*.ntvs
*.njsproj
*.sln
*.sw?

# TypeScript (Caché de compilación incremental)
*.tsbuildinfo
